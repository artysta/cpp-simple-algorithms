#include <cstdlib>
#include <iostream>

using namespace std;

int main(int argc, char * argv[]) {
    char tab[100], ;
    int t;
    srand(time(NULL));

    for (int i = 0; i0; j--) {
        for (int i = 0; i(int) tab[i + 1]) {
            char tmp = tab[i];
            tab[i] = tab[i + 1];
            tab[i + 1] = tmp;
        }
    }
}

for (int i = 0; i & lt; 100; i++) {
    cout & lt; & lt;
    tab[i] & lt; & lt;
    endl;
}

system( & quot; PAUSE & quot;);
return EXIT_SUCCESS;
}